-- Data migration placeholder for one-time `GameEdition` import.
-- Replace this file (or add a new migration) after Phase 1 CSV review by running:
--   pnpm exec tsx src/oneoff/generate-game-edition-migration.run.ts reviewed-editions.csv prisma/migrations/<your_migration>/migration.sql
-- Inserts should populate only "GameEdition"; refresh snapshots separately via bulk-refresh-snapshots.run.ts

SELECT 1;
