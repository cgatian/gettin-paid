# Vite — copy to frontend/.env or frontend/.env.local
# Only VITE_* variables are available in the browser bundle.

# Nest API base URL (client requests go to ${VITE_API_URL}/api/...)
VITE_API_URL=http://localhost:4000
