services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gettin_paid
    ports:
      - "5432:5432"
    volumes:
      - gettin_paid_pg:/var/lib/postgresql/data

volumes:
  gettin_paid_pg:
